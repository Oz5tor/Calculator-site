drop table Kontakt

use Skole
go

create table Kontakt(
	--ID int primary key,
	Navn varchar(255),
	Email varchar(255),
	Besked varchar(255),
);

select * from Users

DELETE FROM Users
WHERE UserID=1;

select * from Kontakt